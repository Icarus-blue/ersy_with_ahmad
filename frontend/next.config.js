/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.youtube.com', 'lh3.googleusercontent.com', 'i.ytimg.com', 'api.unsplash.com', 'images.unsplash.com', 'ersy.com', 't3.gstatic.com'],
    },
};

module.exports = nextConfig;

